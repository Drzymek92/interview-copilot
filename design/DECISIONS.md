# Decision Register (CANONICAL)

> **Note for readers of this public repo.** This project was built with a lightweight in-house
> **design + decision OS**: locked decisions are stated once here (`D#`) and propagated/audited by a
> small governance CLI (`decision_tools.py`) driven by `ROUTINE_*` process docs. **That CLI and its
> framework docs are internal tooling and are *not* part of this release** — this decision log and
> the sibling design docs (`OPEN_DESIGN.md`, `SECURITY_INVARIANTS.md`, `MEASUREMENT_*.md`) are
> included as **design documentation**, so references below to `decision_tools.py`, `ROUTINE_*`,
> `agent/…` or internal file paths point at that internal tooling rather than at files in this repo.

**This file is the single source of truth for every project decision.** Other documents must not
restate a decision — they *apply* it and cite its ID (`D#`). When a decision changes, edit it here
first, then propagate to the listed targets.

- IDs are permanent and never reused. Superseding = mark the old one `SUPERSEDED by D#`, add a new D#.
- `Propagated to` lists every doc that must reflect the decision — it's the sync/coverage checklist.
- Keep statements to 1–2 lines; the *why* lives in the cited rationale note.

<!-- GLANCE  (short labels; single source for the script-generated "at a glance" lines — one line per active D#)
D1=single source of truth
D2=contracts-first anti-rework
D3=scoped context feed
D4=scripted edits, propose-first
D5=design→build→test loop
D6=policy-override waivers
D7=research-sourced coherence checks
D8=conformance over presence
D9=periodic realignment audit
D10=security reconciliation gate
D11=disclosed-only, no stealth
D12=live ambient copilot
D13=session context bundle + interview plan
D14=BYOK cloud LLM default, local fallback
D15=focused Python build, not a fork
D16=STT local GPU faster-whisper
D17=single-box runtime on lab (Ubuntu)
D18=UI local web dashboard (websocket); --app single-app mode owns the recorder + 3 switches
D19=reasoning tails the transcript file
D20=deterministic question trigger
D21=VAD-pause segmentation, no fixed clock
D22=bundle honesty boundary
D23=Salience gate before the suggestion call
D24=Human-referenced accuracy claims only
D25=Provisional lines are the recorder's to write (amends D19)
D26=Per-language span decode on a detected code-switch
D27=Meter allowance sized on the latency STEP, kept at 1.0s
D28=SEGMENT_MAX_SECONDS stays 30s (20s measured)
-->

## Propagation map (decision category → docs that usually need updating)
| Category | Update these (besides this register + INDEX glance + session.md) |
|---|---|
| **SCOPE** (goals, in/out, constraints) | `agent/project.md`, relevant `design/NN` rationale, `OPEN_DESIGN` |
| **ARCHITECTURE** (system shape, components, flow) | `design/NN`, `SPEC_00`, `SPEC` module specs, INDEX |
| **INTERFACE** (data contracts, APIs, schemas) | `SPEC` data contracts, `SPEC` signatures, affected code |
| **DOMAIN** (project/business rules) | the relevant `design/NN` note, `SPEC` as applicable |
| **PROCESS** (workflow, tooling, build approach, conventions) | `SPEC` milestones, `ROUTINE`, `agent/project.md`, INDEX |

Every decision also updates: **this register** + the **INDEX "at a glance" line** (via `sync`) +
`agent/session.md` at session end.

---

## Active decisions

| ID | Date | Cat | Decision | Rationale | Propagated to |
|----|------|-----|----------|-----------|---------------|
| D1 | template | PROCESS | **Single source of truth + propagate:** decisions are stated only here; other docs apply them and cite the `D#`, never restate. Coherence is enforced by `decision_tools.py` (`sync`/`check`/`coverage`). | PRINCIPLES.md | agent/project.md |
| D2 | template | PROCESS | **Contracts-first, anti-rework:** build against final contracts with seams/hooks for unbuilt parts; settle naming/layout/core shapes early. Later stages populate, not restructure. | PRINCIPLES.md, METHODOLOGY.md | agent/project.md · SPEC_00 |
| D3 | 06-23 | PROCESS | **Scoped context feed:** work from a `scope`-resolved working set (the in-play `D#` + only the docs that carry them, with headings), not the whole tree; optional `agent/working_set.md` pin. Cuts tokens + pinpoints decisions. | METHODOLOGY.md | ROUTINE_design_build_test · agent/project.md |
| D4 | 06-23 | PROCESS | **Scripted edits, propose-first:** mechanical propagation/tuning is scripted (`propagate`/`tune`/`stats`) and *printed* by default; it writes only on `--apply`. Keeps determinism + the human's apply gate. | PRINCIPLES.md | ROUTINE_design_build_test · SPEC_01 · agent/project.md |
| D5 | 06-23 | PROCESS | **Design→build→test loop:** an inner loop (`scope`→`new`?→build→`check`/`coverage`→`stats`) gates coherence every iteration, nested in the methodology stages. | METHODOLOGY.md | ROUTINE_design_build_test · agent/project.md |
| D6 | 06-23 | PROCESS | **Policy-override waivers:** consciously-accepted deviations from house coding standards are declared in `project.md`'s Policy Overrides table, so any audit tooling reports them as *overridden*, not as recurring false-positive findings. Safety rules (security/syntax) stay non-waivable. | keeps external audits signal-rich: a waiver recorded once beats re-triaging the same finding every sweep | agent/project.md |
| D7 | 06-23 | PROCESS | **Research-sourced coherence checks:** `check` also flags docs that cite a SUPERSEDED decision (doc↔register inconsistency), active decisions with empty/placeholder rationale, and surfaces accepted-baseline gaps as visible governance debt. | PRINCIPLES.md | ROUTINE_add_decision · agent/project.md |
| D8 | 06-23 | PROCESS | **Conformance over presence (research-grounded):** the governance goal is detecting when a target has *drifted* from the decision it cites, not merely that it cites it (`coverage` = presence today; drift/conformance is the next tier). Methodology + principles are grounded in catalogued literature in `design/REFERENCES.md`. | design/REFERENCES.md, PRINCIPLES.md | design/REFERENCES.md · PRINCIPLES.md · ROADMAP.md |
| D9 | 06-24 | PROCESS | **Periodic realignment audit (high-effort pass):** a cadence-triggered `audit` steps back from the per-loop gate to sweep the *whole* tree — full coherence + baseline-unfiltered coverage debt, a **goal-alignment** matrix (active D# ↔ project goals `O#`: orphan decisions / unaddressed goals), and a deterministic **drift/conformance** check (per-decision content hash → stale citations, the D8 slice). Emits a severity-ranked remediation worklist; propose-first (mechanical fixes on `--apply`, hash restamp on `--update-hashes`). Implements the conformance tier D8 set as direction. | design/REFERENCES.md, PRINCIPLES.md | ROUTINE_coherence_audit · SPEC_01 · METHODOLOGY.md · agent/project.md |
| D10 | 07-19 | PROCESS | **Security-invariant reconciliation gate:** each governed project keeps a canonical `design/SECURITY_INVARIANTS.md` listing its load-bearing security **decision data points** (`SI#`, each citing a source `D#`); **any decision touching a security-sensitive surface — data locality/privacy, transport, credentials/keys, or a remote/command/agent surface — MUST be reconciled against it before implementation**, recording per applicable invariant a one-line `SI# — Reconciled: <how>` / `SI# — N/A: <why>` in the decision's rationale/`design/NN` note. A **🔒 subset is a non-waivable safety floor** (per D6): a decision that can't meet one does not ship — it redesigns or explicitly supersedes the cited decision first. The checklist starts (almost) empty and grows with the project; it **applies** the security-bearing decisions and never restates them (D1). The `ROUTINE_add_decision.md` step makes it a checkable part of the add-decision loop. | Security posture erodes by **drift** — a later feature quietly re-opening a wall an earlier decision closed. Making the invariants an explicit, cited, mostly-non-waivable checklist wired into the routine turns "did we re-check security?" into a **deterministic gate** rather than a thing remembered — the same conformance-over-presence logic D8/D9 apply to coherence, applied to the security posture, at near-zero cost. Piloted in the `odysseus` project (its D21, from the #112 security review) and promoted here so every governed project inherits it. | design/SECURITY_INVARIANTS.md · ROUTINE_add_decision.md · PRINCIPLES.md |

| D11 | 08-28 | SCOPE | **Disclosed-only, no stealth (→ O4):** the tool is used openly where AI is permitted; it ships **no** anti-detection / screen-share-hiding / "invisible" features, and only operates where the specific interview allows AI. The entire covert-cheating branch of this market is out of scope. | documentation_processed/00_research_and_requirements.md §1 | agent/project.md · SECURITY_INVARIANTS.md (SI2) |
| D12 | 08-28 | ARCHITECTURE | **Live ambient copilot (→ O1,O3):** primary mode is an **automatic** background loop (capture → STT → detect question/topic → analyse → display) that surfaces info with **no user input**; the user intervenes only by typing a direct query or picking a suggested option. Manual/quiet mode is secondary. | documentation_processed/00_research_and_requirements.md §2 | agent/project.md · OPEN_DESIGN |
| D13 | 08-28 | INTERFACE | **Per-session context bundle + tracked interview plan (→ O1,O2):** each session loads a structured bundle — JD, company brief, resume, STAR/answer bank, and a step-wise **interview plan** — that primes the reasoning (system prompt + RAG) and is tracked live so the assistant can report plan-state (section, covered, still-owed). "Never start cold." | documentation_processed/00_research_and_requirements.md §2 | agent/project.md · OPEN_DESIGN |
| D14 | 08-28 | ARCHITECTURE | **BYOK cloud LLM default, local fallback (→ O2,O4):** reasoning defaults to a **BYOK cloud** model (Claude/GPT) for quality+latency; **local Ollama** is the offline/practice fallback. Provider keys via env, never hardcoded. SI1 — Reconciled: cloud is an *explicit, non-default-silent* egress of transcript/context; the privacy invariant requires it be opt-in-visible and that local-only mode exists (Ollama + local STT). | documentation_processed/00_research_and_requirements.md §2,§4 | agent/project.md · config/.env.example · SECURITY_INVARIANTS.md (SI1) |

| D15 | 08-28 | PROCESS | **Focused Python build, not a fork (→ O2,O5):** build repo-native in Python (reuse `faster-whisper`, `llm_client`, house standards) rather than forking AnswerCue (TS/Electron). Under the 4-day deadline, an unfamiliar off-stack codebase is higher-risk than a tailored build, and the custom core (D13 bundle + plan tracker + D12 loop) is where the effort belongs. | documentation_processed/00_research_and_requirements.md §3 | agent/project.md · OPEN_DESIGN |
| D16 | 08-28 | ARCHITECTURE | **STT = local GPU faster-whisper (→ O4):** speech-to-text runs locally on the lab RTX 5060 Ti. SI1 — Reconciled: audio never leaves the box for transcription (no cloud-STT egress); satisfies the local-only default. | documentation_processed/00_research_and_requirements.md §4 | agent/project.md · SECURITY_INVARIANTS.md (SI1) |
| D17 | 08-28 | ARCHITECTURE | **Single-box runtime on lab (Ubuntu) (→ O1):** Teams + copilot run on the same lab machine; audio captured via a **PipeWire/PulseAudio monitor source** (interviewer) + separate mic channel. GPU is present at interview time. | documentation_processed/00_research_and_requirements.md §4 | agent/project.md · OPEN_DESIGN |
| D18 | 08-28 | ARCHITECTURE | **UI = local web dashboard over websocket (→ O1,O3):** a FastAPI backend streams transcript + plan-state + suggestions to a browser window (beside Teams). SI1/SI2 — Reconciled: server **binds 127.0.0.1 only** (no external surface), and it is a visible, disclosed panel (no concealment). **Amended 2026-09-07 (#487) — single-app mode (`--app`), folded in here rather than minting a new D#:** the dashboard is also **the one app a desktop shortcut launches**. An `AppController` **owns the recorder lifecycle** (spawns/kills `live_transcribe.py`), and a **loopback-only `POST /control`** endpoint (the websocket stays send-only) drives three on-screen switches — **transcription** (a *true* capture stop: SIGINT the recorder, releasing the mic + ~2.2 GB Whisper VRAM; respawn rotates to a new transcript, followed via a `stop_event`), **suggestions** (a live `reasoning.Controls` flag read per line — off touches no model/GPU while transcript+meter still flow), and **answers local vs local+api** (flips `Controls.backend`). **This does not disturb D19:** the worker still only *tails* the transcript; the controller manages the recorder out of band and never writes the file — spawning a sibling process is not the in-process callback D19 rejected. SI1 preserved: `/control` adds **no** network surface (same 127.0.0.1 bind), and the local+api switch keeps D14's `announce_backend` egress banner (greyed out when cloud is unconfigured, no egress until a suggestion fires). Why fold, not mint: it is the *how* of D18's UI reaching a single launchable app, not a new architectural axis — like D25 amending D19, the seam and its invariants are unchanged. | documentation_processed/00_research_and_requirements.md §2 · #487 (single-app fold, 2026-09-07) | agent/project.md · scripts/dashboard.py · SECURITY_INVARIANTS.md (SI1,SI2) |
| D19 | 09-01 | ARCHITECTURE | **Consumers tail the transcript file (→ O1,O3):** the reasoning layer — and every later consumer, including the D18 dashboard — reads segments by **tailing `scripts/outputs/live_transcript_*.txt`** as `live_transcribe.py` fsyncs each line; the capture loop exposes **no in-process callback** and is not modified to serve consumers. Contract: `[mm:ss-mm:ss] text`, `#` header lines ignored. **Amended by D25** — the recorder may also write provisional lines for the still-open segment to a separate `.partial` file; this file's contract is unchanged. | The recorder holds the one irreplaceable artifact of a live interview, so it must not share a process with a network call that can hang, OOM or raise; a tailing consumer can attach, crash and re-attach mid-call without touching the recording, and a finished transcript replays through the identical code path (which is how #321 was proven without a human). Rejected alternative — an in-process segment callback — is lower latency on paper but couples every future consumer to one process and required editing a script that was proven the day before a real interview. The latency it costs is measured and small: `TRANSCRIPT_POLL_SECONDS=0.25` against a 5–30 s VAD segment cadence is 1–5% of a budget that is already dominated by segment length. SI1 — Reconciled: the seam is a local file read; it adds no egress and no network surface. SI2 — N/A: no concealment surface. | agent/project.md · scripts/reasoning.py · config/settings.py · OPEN_DESIGN (P2) |
| D20 | 09-01 | DOMAIN | **Deterministic question trigger, not an LLM classifier (→ O2,O3):** in ambient mode the LLM fires only on segments a **rule-based pl/en detector** accepts — an explicit `?`, an interrogative in the first three words, or an answer-requesting imperative in the first two — minus a small list of Polish discourse idioms that open with an interrogative and ask nothing. Knobs (`FIRE_ON_QUESTIONS_ONLY`, `SUGGESTION_MIN_WORDS`, `SUGGESTION_COOLDOWN_SECONDS`) are CFG. | CLAUDE.md Determinism First: the rule costs no tokens, adds no latency, cannot hallucinate, and is measurable — `reasoning.py --selftest-heuristic` scores it against 60 hand-labelled lines (P 0.86→1.00 / R 1.00 after the idiom guards; the 1.00 is in-sample and is not claimed as the live rate). An LLM classifier per segment would spend a second model call to beat that, doubling the cost of the loop's cheapest decision. Known limit, recorded not hidden: the transcript is mixed mono with **no speaker attribution**, so the rule cannot yet tell the interviewer's question from the candidate's — deferred to #324. | agent/project.md · scripts/reasoning.py · config/settings.py · tests/fixtures/question_fixtures.json · OPEN_DESIGN (G6) |
| D21 | 09-01 | ARCHITECTURE | **VAD-pause segmentation, fixed-clock chunking disqualified (→ O1):** the live loop closes a segment on a **detected pause** (`SEGMENT_SILENCE_SECONDS`, gated by `ChannelGate` = webrtcvad AND an adaptive per-channel noise floor), never on a timer. `--segmentation fixed` survives only as an A/B harness, and overlap+dedupe is rejected. | Settled by measurement on 2026-08-31 and never closed out until now: identical 37.7 s audio through the live loop gave **VAD pause≥0.9 s → 1.8% WER / 9-10 terms**, fixed 8 s → 5.4%, fixed 15 s → **24.1%** (a content-poor tail window plus forced `pl` hallucinated fluent Polish), fixed 8 s + 1.5 s overlap+dedupe → 44.6%. Fixed cuts bisect the multi-word English phrases that carry the technical terms, and decode latency (rtf ~0.01) never binds, so length is free accuracy. Caveat carried forward: the source was read English TTS with clean sentence pauses, which flatters VAD — it reproduces the mechanism, not the live numbers. SI1/SI2 — N/A: segmentation is local audio handling, no egress and no concealment surface. | agent/project.md · config/settings.py · scripts/live_transcribe.md · OPEN_DESIGN (G8c) |
| D22 | 09-02 | INTERFACE | **Bundle honesty boundary (→ O2,O4):** the context bundle (schema **v2**) carries an `honesty_boundary[]` of `{claim, truth}` rows, and the suggestion prompt enforces them as its **highest-priority rule** — never suggest wording that makes a listed claim, never soften "has not done X" into "has experience with X", and answer a question touching one with the TRUE version. v1 bundles still load; the block is optional. | A copilot that helps the user overclaim is **worse than no copilot**: a gap stated plainly is recoverable, a claim that collapses under one follow-up question ends the process, and the user cannot audit a suggestion mid-answer — they can only read it. So the constraint has to live in the prompt, not in the user's discipline. Verified 2026-09-02 on four trap questions (Qdrant, Airflow/MLflow, an unfinished degree, fine-tuning); all four returned the true version. **Known limit, recorded because it was measured:** the boundary only protects claims that are *listed* — with a full bundle the model still invented a LoRA-vs-full-training trade-off until fine-tuning got its own row, so every new topic area needs one. SI1 — Reconciled: the block is bundle content, local by default like the rest; it adds no egress. SI2 — N/A: it constrains what the tool says to its own user, not what an interviewer can see. | agent/project.md · scripts/reasoning.py · scripts/reasoning.md · scripts/inputs/sessions/<session_id>/bundle.json |
| D23 | 09-04 | DOMAIN | **Salience gate before the suggestion call (→ O2,O3):** a question that clears the D20 trigger fires the suggestion only if a **one-word YES/NO judgement** — "did the interviewer just ask the candidate to say something?" — comes back YES. The judge is the **model already resident for the suggestion** (`SALIENCE_MODEL` empty = `LOCAL_MODEL`), asked with only the segment's **interrogative sentences**, and it **fails open**. The gate sits *after* D20, never instead of it. Knobs (`SALIENCE_GATE_ENABLED`, `SALIENCE_BACKEND`, `SALIENCE_MODEL`, `SALIENCE_THRESHOLD`, `SALIENCE_EMBED_MODEL`, `SALIENCE_TIMEOUT_SECONDS`, `SALIENCE_FAIL_OPEN`) are CFG. **The embed-cosine route this subtask was scoped around is measured and rejected**, kept behind `--salience-backend embed` so the rejection stays reproducible. | Measured on the real 42-min HR screen (2026-09-04). The D20 trigger fired **24** times on that call and most were audio checks, agenda readouts and the interviewer explaining the company. **Why not cosine, which the subtask row settled on:** four variants scored in `--selftest-salience --backend embed` — nomic-embed-text and bge-m3 × whole-segment and question-sentences — and none separates. Two independent reasons. (1) `nomic-embed-text` has no usable Polish: 1/7 top-1 on clean Polish probes against the very topics they were written from, vs **4/4 at 0.57–0.81** for their English translations; `bge-m3` (multilingual) fixes that to 5/7. (2) Fixing the model does not fix the gate — the decisive number is that across the 24 turns the **salient ones average 0.510 and the non-salient 0.518**. The cosine signal is absent and slightly inverted, so no threshold exists to be tuned: best-F1 is 0.55 only by firing 21 of 24. The cause is that an HR screen is **topically saturated** — every turn is about the job — so topical relevance cannot express the thing being asked, which is a **speech act**. **Why the resident model judges:** on 24 labelled turns (8 salient), `interview-copilot:14b` scores **P 0.80 / R 1.00 / F1 0.89**, firing 10/24 at **157 ms median** — against `llama3.2:3b` 0.88/0.88 (123 ms) and `llama3.1:8b` 0.62/1.00 (142 ms). Recall decides it: a wasted call costs tokens, a missed suggestion mid-answer costs the interview. **VRAM decides the model:** the 14B is already loaded, so the gate costs **0 MiB**, whereas a 3B judge beside it measured 13 718 MiB and left **2 273 MiB for a Whisper that needs ~2 200** — not a margin to take into a live interview. Handing the judge only the interrogative sentences is the single biggest accuracy lever measured (3B: F1 0.57 → 0.88), because `SEGMENT_MAX_SECONDS=30` merges a whole exchange into one line. **End-to-end on the real call: 24 → 13 suggestion calls, with all 8 substantive turns still firing** (reproduced twice). Live precision (0.62) is looser than the bench's 0.80 and the gate is slower (520 ms median) because it contends with a streaming suggestion on the same model — both deviations are in the fail-open direction. Caveat carried in the fixture file and printed by the self-test: the 24 turns are **in-sample**, exactly as D20's are. SI1 — Reconciled: the judge is a **local** call to the already-running Ollama, adds no egress, and has no cloud path; a cloud embedding/classifier backend would be a transcript egress and must be reconciled separately, not slipped in. SI2 — N/A: no concealment surface; the gate only reduces what the tool shows its own user. | agent/project.md · scripts/salience.py · scripts/reasoning.py · scripts/reasoning.md · config/settings.py · tests/test_salience.py · tests/fixtures/salience_fixtures.json · OPEN_DESIGN (G6) |
| D24 | 09-04 | PROCESS | **Human-referenced accuracy claims only (→ O4):** this project quotes a **WER / accuracy** figure only when the reference transcript was produced by a **human ear**, and states with it: what was measured, on how many reference words, by whom, and whether the sample was random or adversarial. A second machine decode of the same audio is a legitimate diagnostic but is reported as **divergence**, under that name, never as WER — and a stratified sample's headline is the **random** stratum, with adversarial strata scored separately. Tooling: `scripts/offline_decode.py` (the second decode), `scripts/diff_transcripts.py` (divergence + sampling), `score_transcript.py --sample-pack` (per-stratum scoring, which refuses to print a WER with no reference filled in). | The project has twice mistaken an easier signal for the real one — Day-1 called STT "proven" on one English TTS sentence, and the 3.8% scripted-read figure sat in G8 next to a spontaneous-speech worry it did not address (D21 carries the same caveat). A machine reference would have been the third repeat of that mistake, and this session measured exactly why it fails: at 11:00–11:20 the offline `large-v3` decode hallucinated the same clause **six times** (a `condition_on_previous_text` loop) and scored the live loop 76% "wrong" for a window where the live output was the better transcript. A reference that can be wrong in the direction of the thing being measured is not a reference. The cost of the rule is real and accepted: a human pass is the one step that cannot be automated, so some questions stay open longer — G8's spontaneous WER is open on exactly these grounds, with the sample pack built and waiting rather than filled with a comfortable number. SI1 — Reconciled: the rule forbids the cheap shortcut that would breach it (a cloud STT as "the reference" is a transcript egress); both decodes and all scoring stay local. SI2 — N/A: no concealment surface. | agent/project.md · design/OPEN_DESIGN.md (G8) · scripts/score_transcript.py · scripts/offline_decode.py · scripts/diff_transcripts.py · documentation_processed/_changelog.md |
| D25 | 09-04 | ARCHITECTURE | **Provisional lines are the recorder's to write — amends D19 (→ O1,O3):** `live_transcribe.py` may decode the **still-open** segment on a cadence and write the result to a **separate** `scripts/outputs/live_transcript_*.partial` file that consumers tail exactly as they tail the final transcript; the closing `live_transcript_*.txt` line always supersedes the provisional text, and D19's contract for that file is unchanged. **No consumer loads a second Whisper.** **BUILT 2026-09-05 (#399), both halves.** Knobs (CFG): `PARTIAL_DECODE_ENABLED` / `PARTIAL_DECODE_SECONDS` on the recorder, `DASHBOARD_SHOW_PARTIALS` (`--no-partials`) on the consumer — separate on purpose, so the recorder can keep writing the artefact for the #323 dry-run while a screen chooses not to show it. **The `.partial` file contract, decided here because a file two processes share is an interface:** **append-only**, one line per provisional decode, **the same `[mm:ss-mm:ss] speaker (lang): text` format as the `.txt`** so `follow_transcript`/`parse_transcript_line` read both unchanged, and the **join key is the START stamp** — a provisional shares its start with its final and grows only its right edge. Truncate-and-rewrite was rejected: it is smaller on disk and breaks tailing, which is the whole seam. **On screen** (`scripts/dashboard.py`): one slot, never a transcript line — a provisional does not touch the line counters, the ring buffer, the plan-mention scan or the suggestion-staleness sequence. A final at or past the provisional's start clears it (`==` superseded, `>` orphaned — its segment decoded to nothing), and the clear event is published AFTER the line event so no frame shows neither. **The meter does NOT treat a provisional as an arrival** — P5-A measures the wait for a transcript line, and a provisional is a look at the segment that has not arrived yet. | Measured on the real 42-min HR call 2026-09-04 (probe for #322). **The wait is the whole problem:** median segment **30.0 s**, **54%** (59/110) hit `SEGMENT_MAX_SECONDS`, median screen-still window **28 s**, and a question reaches the screen a median **28.4 s** after it starts being spoken (18/24 triggers over 20 s) while decode is **2.6%** of GPU duty (`0.248 + 0.0144·audio_s`, median 0.59 s, max 0.96 s). **Why a provisional line is not the D21 catastrophe:** 86 growing-prefix decodes of the call's own audio through the live `Transcriber` gave token survival into the final line of **0.86 @5 s / 0.94 @10 s / 0.96 @15–20 s**, with 7/86 below 0.5 and only **1/86** in the low-language-probability forced-`pl` regime. D21 disqualified *fixed-clock chunking*, which cuts **both** edges so a window opens mid-phrase; a growing prefix is anchored at a true speech onset and truncates only its right edge, so that measurement does not transfer — and this is an additional measurement, not a repeal of D21, whose scoring window is untouched here. **The finding that decides the honesty question:** at the 17:53 language switch the provisional was *right* where the final was *wrong* — the +5 s/+10 s decodes rendered the English correctly (`lang=en p=0.99`) while the closing 30 s line forced `pl` and translated it away (G8's straddling-segment failure). So the trade is not "possibly-wrong early text vs an honest blank"; short windows are the ones that get a language switch right. D22's precedent does **not** extend here: it governs generated *suggestions*, which the user cannot audit mid-answer, whereas a provisional *transcript* line is checkable against what the user just heard in the room. **Why this must be the recorder and not a consumer:** the cost is VRAM, not compute — a second Whisper in the dashboard process is ~2.2 GB against the ~2.1 GB the 14B leaves, so it does not fit, while the recorder's model is idle 97.4% of the call and a 5 s cadence adds only ~10% GPU duty (487 decodes ≈ 244 GPU-s). **Why D19's rationale survives the amendment:** D19 keeps the recorder out of *a network call that can hang, OOM or raise* and keeps consumers attachable/detachable behind a file seam. A local decode plus a second local file write is what the recorder already does 110 times a call, and consumers still tail a file — so the seam's shape and the recorder's isolation are preserved; what changes is that the recorder gains one more writer. **MEASURED AT BUILD TIME, 2026-09-05 (lab) — the estimates above held, and one of them was wrong in the project's favour.** A paced replay of the real call's first **600 s** through the live recorder (`--from-wav --pace 1.0 --seconds 600`, a `--pace` flag added because an UNPACED `--from-wav` outruns the GPU ~45x and writes **zero** provisional lines — 414 snapshots displaced — so the offline harness could not exercise the cadence at all): **26 final lines, 90 provisional lines from 91 decodes in 37.43 GPU-s.** **GPU duty: finals 2.3% + provisional 6.2%**, against the ~10% this row estimated. The decode cost model reproduced on fresh data: predicted median 0.392 s / max 0.608 s vs **measured 0.400 / 0.690**. **Every one of the 26 segments got a provisional (median 4 each), 90/90 were superseded, 0 orphaned** — and the screen showed something a **median 16.0 s earlier** than the final line (min 1, max 25), against the 28 s median blank window that is the whole problem. **The delay a provisional imposes on a closing line, which is the number that can hurt: 0 of 28 finals waited behind an in-flight provisional; worst 0.00 s.** That is a small-sample zero, not an impossibility — at 6.2% duty over 28 closings ~1.7 collisions were expected — so the claim that matters is the **bound**, which is structural: the mailbox holds one slot and the worker re-checks the queue after every provisional, so a final can wait behind **at most ONE** provisional decode, measured max **0.690 s**. Worst-case arrival is therefore segment end + one final decode (max 0.85 s) + one provisional (0.69 s) = **1.54 s**, i.e. **0.54 s past the 31.0 s meter ceiling** on a max-length segment — 1.7% of the ceiling, cosmetic to the meter and nothing to the transcript, so it is stated rather than paid for; `METER_DECODE_ALLOWANCE_SECONDS` is #322's lever, not this row's. **D19's contract — and a correction to how it was proven.** Six full replays of the same WAV, 3 with partials on and 3 off: **five are byte-identical and one differs (2 lines, in one 30 s segment)**. The odd one out is a partials-ON run, but the other two partials-ON runs match the partials-OFF group exactly, so **the divergence does not track the flag** — on that segment the decode took **1.85 s vs 1.21 s**, the signature of Whisper's temperature fallback, which SAMPLES. The recorder is not bit-reproducible on hard segments, independently of D25. This supersedes the 2026-09-05 session-10 claim of a byte-identical `.txt`: that was a **single pair**, and a single pair cannot separate "the feature changes nothing" from "the model is nondeterministic" — the first pair drawn in this session would have supported the opposite, and wrong, conclusion. The honest invariant is that partials on/off makes no SYSTEMATIC difference to the `.txt`, at 5/6 agreement across both arms. **Known limit, stated as D24 requires:** survival measures **churn, not accuracy** — no human reference exists for this audio (G8(a) is open, the sample pack built and waiting), the 24 trigger segments are the same in-sample set D20/D23 use, the prefixes were cut at the transcript's segment start so they ignore `SEGMENT_PREROLL_SECONDS`/`SEGMENT_CARRYOVER_SECONDS`, and every number is from **one** HR screen. Whether a provisional line *helps* the user is unmeasured and needs the dry-run. SI1 — Reconciled: the decode is local on the already-loaded model and the provisional text lands in a local file beside the transcript it mirrors; no new egress, and the `.partial` file inherits the D11 delete-after-scoring handling of the artefacts it derives from. SI2 — N/A: no concealment surface; the file is as visible as the transcript. | agent/project.md · design/OPEN_DESIGN.md (P5) · scripts/live_transcribe.py · scripts/dashboard.py · scripts/dashboard_ui.html · config/settings.py |
| D26 | 09-05 | ARCHITECTURE | **Per-language span decode on a detected code-switch (→ O1,O3):** one language per segment is not a decodable assumption for a segment that STRADDLES a language switch, so `stt.transcribe_array` may cut the **audio** at a detected switch and decode each side in its own language (`STT_CODESWITCH_MODE=split`). The switch is detected in **two stages**: a cheap probe of the segment's first and last window on every segment ≥ `STT_CODESWITCH_MIN_SECONDS` (a switch of the utterance's language leaves the two ends on opposite sides of it), and only when those disagree, a sliding scan to locate the boundary. **This does not touch segmentation** — the line keeps its VAD start/end and its single `(lang)` tag, so D21 stands untouched and `SEGMENT_MAX_SECONDS` remains #400's lever. Measured on the real 42-min HR call, all 110 segments, 2+2+2 runs: it changes **exactly the 2 straddling segments and none of the other 108**, repairing both destroyed passages; run-to-run noise floor 0/110. Rejected alternative `rescore` (decode twice, keep the higher `avg_logprob`) still picks ONE language and so trades one half for the other — on [14:39-15:09] it correctly chose `en` for 70% of the audio and translated the Polish opening. **Cost, stated against the P5 meter — CORRECTED 2026-09-05 by D27/#428.** The two-stage probe adds a mean **+0.295 s** to a 30 s segment's close→write (0.680 → 0.975 s, matched A/B on the same 1200 s). It was first reported here as "43/118 max-length lines past the 31.0 s ceiling"; that used a `duration + decode > ceiling` **proxy**, which is not a meter breach — the meter compares an arrival GAP, and a uniform slowdown cancels between consecutive lines (D27). Measured properly on a full-call paced replay: D26 raises the worst back-to-back gap by **+0.27 s** and costs **0 additional false overdues** (1/112 with D26 on, and that one is a Whisper temperature-fallback outlier that would have breached without it; 0/51 vs 0/51 on the matched window). `METER_DECODE_ALLOWANCE_SECONDS` stays **1.0 s** (D27). SI1 — Reconciled: every extra probe and decode runs on the same local model; no new egress. | design/OPEN_DESIGN.md G8 (d) | agent/project.md · scripts/stt.py · config/settings.py · scripts/codeswitch_probe.md |
| D27 | 09-05 | INTERFACE | **The meter's allowance is sized on the latency STEP, and stays 1.0 s (→ O1,O3):** `METER_DECODE_ALLOWANCE_SECONDS` covers the *difference* between consecutive lines' close→write times, **not** a decode time. The meter compares an arrival gap, and `gap = (end(N) − end(N−1)) + (latency(N) − latency(N−1))`, so a **uniform** slowdown cancels and only the *variation* reaches it. Verified on the real call's own line-arrival wall clocks (109 gaps): residual to that model is mean **+0.010 s**, median **−0.001 s**, 87/105 inside the transcript's ±1 s stamp resolution. **Consequences that bind.** (i) A `duration + decode > ceiling` proxy is NOT a meter breach and must not be reported as one — it ignores that the previous line was delayed too, and it is what over-stated D26's cost. (ii) Measured band: nothing changes between **0.75 s and 1.5 s**; below it the knob bites (0.50 s → 4/112 false overdues, 0.25 s → 10/112), above ~2.0 s the ceiling passes the one genuinely anomalous event on the real call — a **2.0 s capture stall** (line 29: gap 32.31 s, spacing 30 s, decode 0.70 s vs 0.38 s), the only residual above rounding noise in 105 gaps. So **do not go below 0.75 s and do not go above 2.0 s**, and never widen it to cover a structural worst case — that worst case (~2.8 s) is unreachable without blinding the meter to the stall it exists to catch. (iii) The residual at 1.0 s is named, not rounded away: **1 false overdue in 112 back-to-back gaps** on a full-call paced replay, overdue by **0.193 s** ≈ 0.8 of one 0.25 s tick — and it is a Whisper temperature-fallback decode outlier (2.020 s against neighbours at 0.83/0.98 s), **not** D26: subtracting D26's uniform probe from both leaves the same step. Matched A/B on the same 1200 s (segments 1–56 identical): D26 raises a 30 s segment's close→write **+0.295 s** (0.680 → 0.975 s) but the worst back-to-back gap only **+0.27 s** (30.30 → 30.57 s), and **both give 0/51 false overdues at 1.0 s**. Forced-collision stress (D25 partials at 1 s, 28/57 finals behind a provisional, worst 0.767 s) also gives **0/51** — frequent collisions hit consecutive lines alike and cancel, so a **rare** collision is the one that shows. Instrument: `live_transcribe.py --latency-trace` → `scripts/meter_budget.py`. | design/OPEN_DESIGN.md P5 | agent/project.md · config/settings.py · scripts/live_transcribe.py · scripts/meter_budget.md |
| D28 | 09-06 | DOMAIN | **`SEGMENT_MAX_SECONDS` stays 30 s — 20 s measured and not adopted (→ O1,O2):** the force-cut cap stays **30 s**; 20 s is a documented CFG knob a user may lower per session, not the default. Measured on the real 42-min call by reproducing the **stereo** segmentation with only the cap changed (`scripts/segment_cap_probe.py`, so the cap is isolated from the mono `--from-wav` downmix/re-segmentation that would otherwise confound it): 30→20 changes **5.7 % of tokens globally / 10.0 % per 20 s window** against a **0/0 %** run-to-run noise floor — a real, large change whose **sign is unmeasured** (both sides Whisper, no human reference — D24/#387). It moves 52 %→**65 %** of segments onto a mid-speech force-cut (toward the regime **D21** disqualified) and raises seam-duplication 2.7 %→**4.5 %**. Its only benefit — a **−10.1 s** median onset→screen latency win — is largely **already bought back by D25** (a truthful provisional shows a median **16 s** earlier), so the marginal value is small. The two costs that ARE clean both survive 20 s: **D26** still fires and repairs both known code-switch straddles (2 passes, both languages kept), and the **D27** meter holds (ceiling 31→21 s, measured min allowance for zero false overdues **0.385 s**, 1.0 s → 0/31). **Reconsider only after** (a) technical-round audio exists (#323 — the cost concentrates on long jargon-dense answers we have not recorded) and (b) a human WER pass resolves the sign (#387). SI1 — Reconciled: all decoding is local; the audio never left the box and the WAV is retained per D11. SI2 — N/A: no concealment surface. | design/MEASUREMENT_segment_cap_400.md | agent/project.md · config/settings.py · design/OPEN_DESIGN.md (P5) · scripts/segment_cap_probe.md |

Example row format (copy when adding — or use `decision_tools.py new`):
```
| D<n> | MM-DD | CATEGORY | **<label>:** <1–2 line statement> | <rationale doc> | <doc> · <doc> |
```

## Superseded decisions
_(when one is superseded, move its row here with "SUPERSEDED by D#".)_
